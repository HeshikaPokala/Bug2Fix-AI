.venv/
__pycache__/
*.py[cod]
.pytest_cache/
frontend/node_modules/
frontend/dist/

# Per-run generated output -- regenerated by every `python3 -m app.main` /
# API / Streamlit run. Not source, shouldn't be committed. See demo/results/
# for the checked-in example outputs used in the demo writeup.
artifacts/
traces/
runtime_inputs/
